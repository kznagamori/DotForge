<Window x:Class="DotForge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DotForge"
        xmlns:vm="clr-namespace:DotForge.ViewModels"
        mc:Ignorable="d"
        Title="DotForge"
        Height="320" Width="600"
        ResizeMode="NoResize"
        Icon="Assets/DotForge.ico">

    <!-- ViewModel -->
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <DockPanel LastChildFill="True">

        <!-- メニューバー -->
        <Grid DockPanel.Dock="Top" Height="20" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Menu Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Top" Height="48" Background="White">
                <MenuItem Header="_ファイル">
                    <MenuItem Header="終了" Click="ExitMenuItem_Click" />
                </MenuItem>
            </Menu>
            <Menu Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Height="48" Background="White">
                <MenuItem Header="_ヘルプ" />
            </Menu>
        </Grid>

        <!-- ステータスバー -->
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding StatusText, Mode=OneWay}"/>
        </StatusBar>

        <!-- コンテンツエリア -->
        <StackPanel Margin="4">

            <!-- 1行目: テンプレート (Label + ComboBox) [ボタン無し] -->
            <DockPanel Margin="4">
                <!-- 左端：Label (固定幅) -->
                <Label DockPanel.Dock="Left"
                       Content="テンプレート："
                       Width="120"
                       VerticalAlignment="Center"
                       IsEnabled="{Binding IsTemplateRowEnabled}" />

                <!-- 中央：ComboBox (固定幅) -->
                <ComboBox DockPanel.Dock="Left"
                          Width="360"
                          ItemsSource="{Binding Templates}"
                          SelectedItem="{Binding SelectedTemplate, Mode=TwoWay}"
                          IsEnabled="{Binding IsTemplateRowEnabled}" />

                <!-- 右端：ボタンが無い代わりにダミー要素で幅を確保（Width=80） -->
                <Rectangle DockPanel.Dock="Right"
                           Width="80"
                           Fill="Transparent"/>
            </DockPanel>

            <!-- 2行目: プロジェクトファイル (Label + TextBox + 開くボタン) -->
            <DockPanel Margin="4">
                <Label DockPanel.Dock="Left"
                       Content="プロジェクトファイル："
                       Width="120"
                       VerticalAlignment="Center"
                       IsEnabled="{Binding IsProjectFileRowEnabled}" />

                <TextBox DockPanel.Dock="Left"
                         Width="360"
                         Text="{Binding ProjectFilePath, Mode=TwoWay}"
                         IsEnabled="{Binding IsProjectFileRowEnabled}" />

                <!-- ボタンあり（幅80） -->
                <Button DockPanel.Dock="Right"
                        Width="80"
                        Content="開く"
                        Command="{Binding OpenProjectFileCommand}"
                        IsEnabled="{Binding IsProjectFileRowEnabled}" />
            </DockPanel>

            <!-- 3行目: プロジェクト名 (Label + TextBox) [ボタン無し] -->
            <DockPanel Margin="4">
                <Label DockPanel.Dock="Left"
                       Content="プロジェクト名："
                       Width="120"
                       VerticalAlignment="Center"
                       IsEnabled="{Binding IsProjectNameRowEnabled}" />

                <TextBox DockPanel.Dock="Left"
                         Width="360"
                         Text="{Binding ProjectName, Mode=TwoWay}"
                         IsEnabled="{Binding IsProjectNameRowEnabled}" />

                <!-- 右端：ボタンが無いのでダミー要素で幅を確保 -->
                <Rectangle DockPanel.Dock="Right"
                           Width="80"
                           Fill="Transparent"/>
            </DockPanel>

            <!-- 4行目: クラス名 (Label + TextBox) [ボタン無し] -->
            <DockPanel Margin="4">
                <Label DockPanel.Dock="Left"
                       Content="クラス名："
                       Width="120"
                       VerticalAlignment="Center"
                       IsEnabled="{Binding IsClassNameRowEnabled}" />

                <TextBox DockPanel.Dock="Left"
                         Width="360"
                         Text="{Binding ClassName, Mode=TwoWay}"
                         IsEnabled="{Binding IsClassNameRowEnabled}" />

                <Rectangle DockPanel.Dock="Right"
                           Width="80"
                           Fill="Transparent"/>
            </DockPanel>

            <!-- 5行目: 出力先ディレクトリ (Label + TextBox + ボタン) -->
            <DockPanel Margin="4">
                <Label DockPanel.Dock="Left"
                       Content="出力先ディレクトリ："
                       Width="120"
                       VerticalAlignment="Center"
                       IsEnabled="{Binding IsOutputDirRowEnabled}" />

                <TextBox DockPanel.Dock="Left"
                         Width="360"
                         Text="{Binding OutputDirectory, Mode=TwoWay}"
                         IsEnabled="{Binding IsOutputDirRowEnabled}" />

                <Button DockPanel.Dock="Right"
                        Width="80"
                        Content="開く"
                        Command="{Binding OpenOutputDirCommand}"
                        IsEnabled="{Binding IsOutputDirRowEnabled}" />
            </DockPanel>

            <!-- 6行目: 出力ボタン（右寄せ） -->
            <DockPanel Margin="4">
                <!-- Label/TextBox は不要なのでダミー要素。幅=120+250=370ぶん確保しておいてもOK -->
                <Rectangle DockPanel.Dock="Left"
                           Width="400"
                           Fill="Transparent"/>

                <Button DockPanel.Dock="Right"
                        Width="160"
                        Content="出力"
                        Padding="10,5"
                        Command="{Binding GenerateOutputCommand}"
                        IsEnabled="{Binding IsOutputEnabled}" />
            </DockPanel>

        </StackPanel>
    </DockPanel>
</Window>
